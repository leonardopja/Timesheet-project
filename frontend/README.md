# Manage My Shifts

This is a client-only application built with HTML, CSS and vanilla JavaScript. It does not require a framework, server or database.

## Run locally

Open `index.html` directly in a browser, or serve the `frontend` folder with any static file server:

```bash
npx serve .
```

All users, sessions and shifts are stored in the browser's `localStorage`.
